{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build tests",
            "type": "shell",
            "command": "bazel build --config debug //:rel_test",
            "group": {
                "kind": "build",
                "isDefault": false
            }
        }
    ]
}

{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build tests",
            "type": "shell",
            "command": "bazel build --config debug //tests:all",
            "group": {
                "kind": "build",
                "isDefault": false
            }
        }
    ]
}

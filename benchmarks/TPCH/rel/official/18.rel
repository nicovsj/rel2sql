// Generated by scripts/tpch_official_emit.py from ../ai-rewrites/18.rel -- do not hand-edit.
// Ready for rel2sql as-is:
//   rel2sql_bin -e benchmarks/TPCH/rel/tpch_edb.edb -f benchmarks/TPCH/rel/official/18.rel

// Q18 -- Large Volume Customer
//   orders whose total quantity exceeds a threshold, with customer details.
//   The per-order total is its own def and is reused for both the filter and the
//   reported quantity, so the aggregate is built once.

def order_qty {
    [o]: sum[(num, q): l_quantity(o, num, q)]
}
def result {
    (name, c, o, od, price, qty):
    order_qty(o, qty) and qty > 300 and
    o_custkey(o, c) and c_name(c, name) and
    o_orderdate(o, od) and o_totalprice(o, price)
}
